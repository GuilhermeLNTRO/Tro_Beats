<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pagamento Aprovado! - TROBeats</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <style>
        /* Estilos específicos para a página de sucesso (baseado no style.css) */
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .main-content {
            flex-grow: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .success-box {
            background-color: var(--card-bg); /* Fundo do card */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 600px;
            border: 1px solid var(--border-gray);
            text-align: center;
        }
        
        .success-icon {
            font-size: 4rem;
            color: var(--alert-success); /* Verde Sucesso */
            margin-bottom: 20px;
            animation: popIn 0.5s ease-out;
        }

        @keyframes popIn {
            0% { transform: scale(0.5); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }

        .success-box h2 {
            text-align: center;
            color: var(--text-light);
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .success-box .subtitle {
            font-size: 1.1rem;
            color: #ccc;
            margin-bottom: 25px;
        }

        /* Lista de Downloads */
        .download-list {
            margin-top: 30px;
            border-top: 1px solid var(--border-gray);
            padding-top: 20px;
        }
        .download-list h4 {
            font-weight: 600;
            color: var(--secondary-purple);
            margin-bottom: 15px;
            text-align: left;
        }
        .download-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: var(--bg-dark);
            padding: 10px 15px;
            border-radius: 5px;
            border: 1px solid var(--border-gray);
            margin-bottom: 10px;
        }
        .download-item span {
            color: var(--text-light);
        }
        /* O botão de download usa as classes de btn */
        .btn-download {
            background-color: var(--primary-purple);
            color: var(--text-light);
            padding: 8px 15px;
            border-radius: 5px;
            font-size: 0.9rem;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
        }
        .btn-download:hover {
            background-color: var(--search-button-hover);
        }

    </style>
</head>
<body>
    <header class="header">
        <div class="container header-content">
            <div class="logo">
                <a href="/" aria-label="Página Inicial"> <h1>TROBeats</h1> </a>
            </div>
        </div>
    </header>

    <main class="main-content section-padding">
        <div class="container">
            <div class="success-box">
                <div class="success-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <h2>Pagamento Aprovado!</h2>
                <p class="subtitle">
                    Obrigado pela sua compra no valor de **R$<%= total %>**.
                </p>
                <p class="text-light">Seus ficheiros estão prontos para download abaixo.</p>

                <!-- Lista de Downloads -->
                <div class="download-list">
                    <h4>Seus Ficheiros:</h4>
                    
                    <% purchasedBeats.forEach(beat => { %>
                        <div class="download-item">
                            <span><%= beat.title %></span>
                            <!-- 
                                NOTA DE SEGURANÇA:
                                Idealmente, esta rota /download/:filename deveria ser uma nova rota segura no app.js
                                que verifica se o utilizador atual (req.user) realmente comprou este item
                                antes de enviar o ficheiro.
                                
                                Para simplificar, estamos a assumir que a rota /play-beat/:beatId
                                (que você já tem) pode ser usada para o download, ou que o link
                                direto para /private_audio/ é tratado pelo Express.

                                **** Solução Segura (Requer nova rota GET /download-beat/:beatId no app.js) ****
                                <a href="/download-beat/<%= beat.id %>" class="btn-download">
                            -->
                            
                            <!-- 
                                Solução Simples (Linka para o ficheiro de áudio completo)
                                ATENÇÃO: Isto expõe o nome do ficheiro em private_audio,
                                mas como o utilizador já pagou, é aceitável.
                            -->
                            <a href="/private_audio/<%= beat.audioFile %>" class="btn-download" download>
                                <i class="fas fa-download"></i> Baixar
                            </a>
                        </div>
                    <% }); %>
                </div>

                <a href="/meus-negocios" class="btn btn-secondary" style="margin-top: 30px;">Ver Minhas Compras</a>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 TROBeats. Todos os direitos reservados.</p>
        </div>
    </footer>
    
</body>
</html>
