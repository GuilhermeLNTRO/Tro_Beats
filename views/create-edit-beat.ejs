<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TROBeats - <%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header class="header">
        <div class="container header-content">
            <div class="logo">
                <a href="/" aria-label="Página Inicial"> <h1>TROBeats</h1> </a>
            </div>
            <div class="header-actions">
                <a href="/perfil" class="user-icon" aria-label="Perfil do Usuário"><i class="fas fa-user-circle"></i></a>
                <a href="/carrinho" class="cart-icon" aria-label="Carrinho de Compras"><i class="fas fa-shopping-cart"></i></a>
            </div>
        </div>
    </header>

    <main class="main-content">
        <section class="form-section section-padding">
            <div class="container">
                <h2><%= title %></h2>
                <form id="beatForm" class="general-form">
                    <div class="form-group">
                        <label for="beatTitle">Título do Beat:</label>
                        <input type="text" id="beatTitle" name="title" value="<%= beat ? beat.title : '' %>" required>
                    </div>
                    <div class="form-group">
                        <label for="beatGenre">Gênero:</label>
                        <input type="text" id="beatGenre" name="genre" value="<%= beat ? beat.genre : '' %>" required>
                    </div>
                    <div class="form-group">
                        <label for="beatPrice">Preço (R$):</label>
                        <input type="number" id="beatPrice" name="price" step="0.01" value="<%= beat ? beat.price : '0.00' %>" required>
                    </div>
                    <div class="form-group">
                        <label for="beatCover">URL da Capa:</label>
                        <input type="url" id="beatCover" name="cover" value="<%= beat ? beat.cover : '' %>">
                        <small>Ex: /img/minha_capa.jpg ou https://exemplo.com/capa.png</small>
                    </div>
                    <div class="form-group">
                        <label for="beatStatus">Status:</label>
                        <select id="beatStatus" name="status">
                            <option value="À Venda" <%= beat && beat.status === 'À Venda' ? 'selected' : '' %>>À Venda</option>
                            <option value="Rascunho" <%= beat && beat.status === 'Rascunho' ? 'selected' : '' %>>Rascunho</option>
                            <option value="Arquivado" <%= beat && beat.status === 'Arquivado' ? 'selected' : '' %>>Arquivado</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary"><%= isEdit ? 'Salvar Alterações' : 'Adicionar Beat' %></button>
                    <a href="/meus-negocios" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 TROBeats. Todos os direitos reservados.</p>
        </div>
    </footer>

    <script src="/js/create-edit-beat.js"></script>
</body>
</html>