<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TROBeats - Login</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header class="header">
        <div class="container header-content">
            <div class="logo">
                <h1>TROBeats</h1>
            </div>
            <nav class="main-nav">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/catalogo">Catálogo</a></li>
                    <li><a href="/perfil">Perfil</a></li>
                    <li><a href="/carrinho">Carrinho</a></li>
                </ul>
            </nav>
            <div class="header-actions">
                <div class="search-bar">
                    <input type="text" placeholder="Procurar Trap ou Drill ou Juice Wrld...">
                    <button aria-label="Pesquisar"><i class="fas fa-search"></i></button>
                </div>
                <a href="/vender" class="btn btn-primary">Começar a Vender</a>
                <a href="/register" class="btn btn-secondary">Cadastre-se</a>
                <a href="/login" class="user-icon" aria-label="Perfil do Usuário"><i class="fas fa-user-circle"></i></a>
                <a href="/carrinho" class="cart-icon" aria-label="Carrinho de Compras"><i class="fas fa-shopping-cart"></i></a>
            </div>
        </div>
    </header>

    <main class="main-content form-page">
        <section class="login-section section-padding">
            <div class="container form-container">
                <h2>Faça Login na TROBeats</h2>

                <% if (messages && messages.error) { %>
                    <div class="alert error-message">
                        <%= messages.error %>
                    </div>
                <% } %>
                <% if (messages && messages.success) { %>
                    <div class="alert success-message">
                        <%= messages.success %>
                    </div>
                <% } %>

                <form action="/login" method="POST" class="login-form"> <div class="form-group">
                        <label for="email">Email:</label>
                        <input type="email" id="email" name="email" placeholder="seuemail@exemplo.com" required value="<%= typeof formData !== 'undefined' && formData.email ? formData.email : '' %>">
                    </div>

                    <div class="form-group">
                        <label for="password">Senha:</label>
                        <input type="password" id="password" name="password" placeholder="Sua senha" required>
                    </div>

                    <button type="submit" class="btn btn-primary btn-full-width">Entrar</button>

                    <p class="form-footer-text">Não tem uma conta? <a href="/register">Cadastre-se aqui!</a></p> </form>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 TROBeats. Todos os direitos reservados.</p>
        </div>
    </footer>

    <script src="/js/login.js"></script> </body>
</html>